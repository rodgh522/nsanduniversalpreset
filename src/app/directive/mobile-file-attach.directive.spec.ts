import { MobileFileAttachDirective } from './mobile-file-attach.directive.tns';

describe('MobileFileAttachDirective', () => {
  it('should create an instance', () => {
    const directive = new MobileFileAttachDirective();
    expect(directive).toBeTruthy();
  });
});
