import { CommaDirective } from './comma.directive';

describe('CommaDirective', () => {
  it('should create an instance', () => {
    const directive = new CommaDirective();
    expect(directive).toBeTruthy();
  });
});
